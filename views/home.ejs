<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
</head>
<body>
    <h1>My Employees</h1>
    <button type="button" onclick="location.href='/employees/add'">Add Employee</button>
    <br>
    <br>
    <p>Sort By</p>
    <form method="get" action="/">
        <button type="submit" name="sort" value="name">Name</button>
        <button type="submit" name="sort" value="position">Position</button>
    </form>
    <br>
    <br>
    <form method="get" action="/">
        <select name="filter">
            <option selected disabled>Filter by Degree</option>
            <option value="Bachelor Degree">Bachelor Degree</option>
            <option value="Master Degree">Master Degree</option>
        </select>
        <button type="submit">Filter</button>
    </form>
    <br>
    <br>
    <% for (let i = 0; i < data.length; i++) { %>
        <div class="card" style="width: 18rem;">
            <img src="<%= data[i].profile_picture %>" class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title"><%= data[i].name %> (<%= data[i].age %>)</h5>
              <h7><%= data[i].position %></h7><br>
              <h7>(<%= data[i].education %>)</h7><br>
              <hr>
              <div class="card-text">
                  <p>Contact</p>
                  <p><%= data[i].email %></p>
                  <p><%= data[i].phone_number %></p>
              </div>
              <a href="/employees/edit/<%= data[i].id %>" class="btn btn-primary">Edit</a>
              <a href="/employees/delete/<%= data[i].id %>" class="btn btn-primary">Delete</a>
            </div>
        </div>
   <% } %>
   <br>
   <a href="/logout">Log Out</a>
</body>
</html>